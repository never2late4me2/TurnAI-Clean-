version: "3.9"

services:
  turnai-clean:
    build: .
    container_name: turnai-clean
    ports:
      - "8000:8000"
    environment:
      - STRIPE_SECRET_KEY=${STRIPE_SECRET_KEY}
      - DATABASE_URL=sqlite:///./turnai_full_utils.db
    volumes:
      - ./data:/app/data
